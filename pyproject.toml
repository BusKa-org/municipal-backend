[project]
name = "buska-backend"
version = "0.1.0"
description = "Backend API for BusKá - School Bus Management System"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "flasgger>=0.9.7.1",
    "flask>=3.1.2",
    "flask-cors>=6.0.1",
    "flask-jwt-extended>=4.7.1",
    "flask-restx>=1.3.0",
    "flask-sqlalchemy>=3.1.1",
    "geoalchemy2>=0.18.0",
    "marshmallow>=3.20.1",
    "gunicorn>=23.0.0",
    "psycopg2-binary>=2.9.11",
    "python-dotenv>=1.2.1",
    "python-json-logger>=2.0.7",
    "sqlalchemy>=2.0.44",
    "alembic>=1.13.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=9.0.2",
    "pytest-cov>=7.0.0",
    "pytest-mock>=3.14.0",
    "faker>=26.0.0",
    "factory-boy>=3.3.0",
    "requests>=2.32.0",
    "pytest-xdist>=3.6.0",
    "black>=24.0.0",
    "ruff>=0.2.0",
    "mypy>=1.8.0",
    "pre-commit>=3.6.0",
]

# ==========================================
# Tool Configurations
# ==========================================

[tool.black]
line-length = 100
target-version = ["py312"]
exclude = '''
/(
    \.git
    | \.venv
    | migrations
    | __pycache__
)/
'''

[tool.ruff]
line-length = 100
target-version = "py312"
exclude = [".venv", "migrations"]

[tool.ruff.lint]
select = [
    "E",      # pycodestyle errors
    "W",      # pycodestyle warnings
    "F",      # pyflakes
    "I",      # isort
    "B",      # flake8-bugbear
    "UP",     # pyupgrade
]
ignore = [
    "E501",   # line too long (handled by black)
    "B904",   # raise-without-from-inside-except (intentional: converting to user-friendly errors)
]

[tool.mypy]
python_version = "3.12"
warn_return_any = false  # Too noisy with SQLAlchemy queries
warn_unused_ignores = true
ignore_missing_imports = true
check_untyped_defs = false  # Don't enforce types in untyped functions
exclude = ["migrations", ".venv"]

# Ignore Flask-SQLAlchemy model issues (needs proper stubs)
[[tool.mypy.overrides]]
module = "app.models.*"
ignore_errors = true

# Services - relaxed but still useful checking
[[tool.mypy.overrides]]
module = "app.services.*"
warn_return_any = false
check_untyped_defs = false

# Stricter checking on core and new code
[[tool.mypy.overrides]]
module = "app.core.*"
warn_return_any = true
check_untyped_defs = true

[[tool.mypy.overrides]]
module = "app.api.controllers.*"
warn_return_any = false

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]
addopts = "-ra -v --tb=short --strict-markers --cov=app --cov-report=term-missing --cov-report=html --cov-branch"
markers = [
  "unit: fast tests without DB",
  "integration: API/DB integration tests",
  "e2e: full flows, slowest",
]
filterwarnings = [
  "ignore:pythonjsonlogger\\.jsonlogger has been moved:DeprecationWarning",
]

[tool.coverage.run]
branch = true
omit = [
    "*/tests/*",
    "*/__pycache__/*",
    "*/venv/*",
    "*/.venv/*",
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
packages = ["app"]
