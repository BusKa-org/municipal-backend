terraform.tfstate
terraform.tfstate.backup
.terraform/
crash.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json
# The lock file pins provider versions and is meant to be committed, so it is
# explicitly kept. It is already tracked; this stops it being dropped later.
!.terraform.lock.hcl
terraform.tfvars
terraform.tfvars.json
clouds.yaml
