# Buska Backend - Ansible Inventory
# Defina seus hosts e variáveis aqui

[localhost]
127.0.0.1 ansible_connection=local

[openstack_vms]
# Exemplo:
# buska_vm ansible_host=10.0.0.10 ansible_user=ubuntu ansible_ssh_private_key_file=~/.ssh/id_rsa

# Grupo de variáveis
[openstack_vms:vars]
# vault_db_password=seu_db_password (use ansible-vault)
# vault_jwt_secret=seu_jwt_secret (use ansible-vault)
